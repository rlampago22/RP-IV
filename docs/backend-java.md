# Backend — Java (EDA)

Código de domínio e EventBus.

## Estado atual

A implementação executável do MVP está em [`../mvp/`](../mvp/) (legado Semana 2 — inclui Swing).

## Direção

Migrar gradualmente o domínio Java de `mvp/src/...` e `src/main/java/...` para esta pasta (`backend/`), expondo API HTTP para o `frontend/` React.

## Trilhas (entrega vertical)

| Trilha | Pacotes |
|--------|---------|
| Bernardo | ControleReator, medições |
| Bruno | EventBus |
| José | Alarmes, Strategy |
| Marcus | AuditoriaLogs |
| Álvaro | Aceite / contratos API |
