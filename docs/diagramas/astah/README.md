# Astah — diagramas UML (fonte)

## Arquivos

- `Diagrama-Casos-Uso-Usina-Nuclear.asta` — abrir no Astah UML / Astah Professional / Community
- `EntityStore` — store interno (já empacotado no `.asta`)

## Origem

Exportado a partir do zip em Downloads (`Diagrama de Caso de Usos - Usina Nuclear.asta.zip`).  
O arquivo `.asta` de 1 byte no Downloads era placeholder (OneDrive/nuvem) e **não** abre.

## Uso no RP IV

Este Astah reflete o modelo **amplo da APS**. Para Marco 1, preferir o recorte MVP em PlantUML:

- `docs/marco1/diagramas/casos-de-uso-mvp.puml`
- demais `.puml` em `docs/marco1/diagramas/`

Atualizar o Astah só quando for necessário evidência visual; manter consistência com o MVP.
